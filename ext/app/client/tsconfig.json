{
  "extends": "../../../buildtools/tsconfig-base-ext.json",
  "include": [
    "**/*",
    // Added for typechecks. Not sure what other paths mean here as all are invalid.
    "../../../core/app/client/**/*",
    // TODO: explain those, as they are invalid, probably they are valid when moved around.
    "../../../app/client/**/*",
    "../../../stubs/app/client/ui/DeleteAccountDialog.ts",
    "../../../stubs/app/client/ui/ChangePasswordDialog.ts",
    "../../../stubs/app/client/ui/MFAConfig.ts"
  ],
  "references": [
    { "path": "../common" },
  ]
}
